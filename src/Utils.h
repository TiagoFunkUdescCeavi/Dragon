#ifndef UTILS_H
#define UTILS_H

#include <string>

using namespace std;

string charToString( char c );
bool isChar( char c );
bool isLetter( char c );
bool isDigit( char c );
bool isSpecialChar( char c );
bool isBlank( char c );

#endif
